using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
static public class LogItemFactory
{
    static public Bopper.LogItem MakeLogItem(Command command)
    {
		System.Type type = command.GetType();

		if (type == typeof(CommandSay))
			return new Bopper.ChatLogItem(command.player_id, command.ToString());

		else if (type == typeof(CommandPhase))
			return new Bopper.PhaseLogItem(command.player_id, command.ToString());

		else
			return new Bopper.CommandLogItem(command.player_id, command.ToString());
	}
}

*/